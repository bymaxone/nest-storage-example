/**
 * @fileoverview Next.js 16 configuration — ESM, no extra plugins needed for
 * the local @bymax-one/nest-storage file: dependency (the shared subpath is
 * pure TypeScript/JS, so no special transpile config is required).
 * @module next.config
 */
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {}

export default nextConfig
