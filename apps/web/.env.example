# Environment template for the nest-storage-example web dashboard.
# Copy to `.env` and adjust as needed; the committed default targets the local API.

# Base URL of the API the dashboard talks to (exposed to the browser bundle).
NEXT_PUBLIC_API_URL=http://localhost:3001
